package rutebaga.test.commons;

import rutebaga.commons.math.EllipseBounds2D;
import rutebaga.commons.math.Matrix;
import rutebaga.commons.math.Vector2D;

public class CircleBoundsTest
{
	/*
	public static void main(String... args)
	{
		EllipseBounds2D bounds = new EllipseBounds(new Vector2D(0, 0), new Vector2D(
				5, 5));
		double[][] coords =
		{
		{ 0, 0 },
		{ 1, 1 },
		{ 6, 1 },
		{ 5, 5 },
		{ 6, 6 } };
		Vector2D[] vectors = new Matrix(coords).asVectors();
		for (Vector2D v : vectors)
		{
			System.out.print(bounds.contains(v) + " for ");
			MatrixTest.print(v);
		}
	}
	*/
}
